// This is a FRONTEND MODEL (before MongoDB)

const upcomingCourses = {
  code: String,
  title: String,
  dates: String,
  duration: String,
  centre: String,
  image: String,
  status: "Applications Open",
};

export default upcomingCourses;
